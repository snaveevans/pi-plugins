{
  "name": "pi-session-loop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Re-run a prompt in this Pi session on an interval. Lightweight /loop, not a cloud scheduler.",
  "keywords": ["pi-package"],
  "license": "MIT",
  "pi": {
    "extensions": ["./extensions"]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  }
}
